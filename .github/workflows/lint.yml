name: Lint

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build SSG
        run: pnpm run build

      - name: Run HTMLHint
        run: pnpm run lint:html

      - name: Run Stylelint
        run: pnpm run lint:css

      - name: Run ESLint
        run: pnpm run lint:js
        # ESLint 配置已优化，警告不会导致失败
        # 详见 docs/LINT_WARNINGS.md

      - name: Run tests
        run: pnpm test

      - name: Check tools sync
        run: |
          pnpm run sync:tools
          if [ -n "$(git diff --name-only index.html)" ]; then
            echo "❌ Error: index.html is out of sync with tools.json"
            echo ""
            echo "Please run locally:"
            echo "  pnpm run sync:tools"
            echo "  git add index.html && git commit --amend --no-edit"
            echo "  git push --force-with-lease"
            echo ""
            echo "Diff:"
            git diff index.html | head -50
            exit 1
          fi
          echo "✅ Tools are in sync"
